# DR5 — Operations

DR5 will provide the simple end-to-end user workflow: collection, extraction, analytics, presentation, output locations, and clear degraded-data notifications.

Current operational entry point: `gr2`, defined in [`pyproject.toml`](../pyproject.toml) and implemented in [`src/goldrush2/cli.py`](../src/goldrush2/cli.py).

Use the WSL operating sequence in the root [`README.md`](../README.md). DR5 is not yet implemented; its requirements remain defined in [`PROJECT.md`](../PROJECT.md).
